# Role
你是一名精通 Java Spring Boot 3.x, MyBatis 和 JSP 的全栈开发架构师。
我正在开发 "Memory-Assistant" 记忆卡片项目，目前项目已有基础的卡组和卡片 CRUD 功能。

# Context (项目背景)
1.  **现有表结构**: 包含 `sys_user` (用户), `flashcard_deck` (用户卡组), `flashcard` (用户卡片), `review_log` (复习日志)。
2.  **技术栈**: Spring Boot + MyBatis (XML) + JSP + Bootstrap 5 + MySQL。
3.  **新需求**: 我需要新增一个 **“VIP 卡组商店”** 功能。
    -   系统中存在一些“官方/VIP卡组”（如“托福核心词汇”、“Java面试高频题”）。
    -   用户可以在“商店页面”浏览这些卡组。
    -   用户点击“购买”（模拟支付成功）后，系统自动将该 VIP 卡组及其包含的所有卡片，**完整复制**一份到该用户的账号下。
    -   复制后的卡组，属于该用户，用户可以随意修改，且初始复习进度需重置。

# Step 1: Database Design (Extension)
请基于现有表结构，为我生成**新增的 SQL 脚本** (`market_init.sql`)。
为了不污染现有的用户数据表，请新建两张表专门存储商店数据：
1.  `sys_market_deck`: 结构类似于 `flashcard_deck`，但没有 `user_id`（因为它是公共商品），增加 `price` (价格) 和 `cover_url` (封面图) 字段。
2.  `sys_market_card`: 结构类似于 `flashcard`，关联 `sys_market_deck`。
3.  **重要**: 请在 SQL 脚本末尾生成 2 个测试用的 VIP 卡组和每个卡组对应的 3-5 张测试卡片数据，方便我直接导入测试。

# Step 2: Backend Implementation
请实现核心业务逻辑：

1.  **Entity**: 生成 `MarketDeck` 和 `MarketCard` 实体类。
2.  **Mapper**: 生成 `MarketMapper` (接口和 XML)，用于查询商店列表和获取商店卡片详情。
3.  **Service (核心难点)**:
    -   创建 `MarketService.java`。
    -   实现 `buyDeck(Long userId, Long marketDeckId)` 方法。
    -   **深拷贝逻辑 (Deep Copy Logic)**:
        1.  根据 `marketDeckId` 查出商品卡组信息。
        2.  **Insert Deck**: 向 `flashcard_deck` 表插入一条新数据：
            -   `user_id` = 当前购买者 `userId`
            -   `deck_name` = 商品名称
            -   `description` = 商品描述
            -   `status` = 1
        3.  根据 `marketDeckId` 查出该商品下的所有 `sys_market_card`。
        4.  **Batch Insert Cards**: 遍历这些卡片，构建 `Flashcard` 对象列表：
            -   `deck_id` = 刚才新生成的卡组 ID
            -   `question` = 商品卡片的问题
            -   `answer` = 商品卡片的答案
            -   `next_review_date` = `LocalDate.now()` (今天)
            -   `stage` = 0 (初始阶段)
            -   `review_count` = 0
        5.  调用现有的 `FlashcardMapper.batchInsert` (如果已有) 或循环调用 insert 将卡片写入数据库。
4.  **Controller**:
    -   `MarketController`: 处理 `/market` (列表页) 和 `/market/buy/{id}` (购买动作)。
    -   购买成功后，重定向到 `/decks` (我的卡组页) 并带上 `success` 提示。

# Step 3: Frontend Implementation
请编写前端页面：

1.  **market.jsp**:
    -   使用 Bootstrap 5 的 Card Grid 布局展示 VIP 卡组。
    -   显示封面、名称、描述、价格（例如：￥9.99）。
    -   “购买”按钮：点击后弹出 `confirm` 确认框：“确认支付 ￥xx 购买此卡组吗？”，确认后提交 POST 请求到后端。
2.  **Navbar Update**:
    -   请告诉我如何在导航栏代码中添加“VIP 商店”的入口链接。

# Output Constraints (输出要求)
1.  **SQL 脚本**: 必须包含 `CREATE TABLE` 和 `INSERT INTO` 测试数据，只生成脚本即可，我自己在数据库中执行。
2.  **代码完整性**: `MarketService` 中的**数据复制逻辑**是核心，请写得非常详细，确保不要漏掉字段映射。
3.  **不破坏现有代码**: 不要修改已有的 `FlashcardService` 或 `DeckController`，所有新逻辑尽量独立。
以下是数据库的代码：create table sys_user
          (
              id          bigint auto_increment comment '用户ID'
                  primary key,
              username    varchar(50)                        not null comment '用户名',
              password    varchar(100)                       not null comment '密码',
              email       varchar(100)                       null comment '邮箱',
              nickname    varchar(50)                        null comment '昵称',
              avatar_url  varchar(255)                       null comment '头像URL',
              create_time datetime default CURRENT_TIMESTAMP null comment '创建时间',
              update_time datetime default CURRENT_TIMESTAMP null on update CURRENT_TIMESTAMP comment '更新时间',
              status      tinyint  default 1                 null comment '状态: 0-禁用, 1-启用',
              constraint username
                  unique (username)
          )
              comment '用户表' charset = utf8mb4;

          create table flashcard_deck
          (
              id          bigint auto_increment comment '卡组ID'
                  primary key,
              user_id     bigint                             not null comment '所属用户ID',
              deck_name   varchar(100)                       not null comment '卡组名称',
              description text                               null comment '卡组描述',
              category    varchar(50)                        null comment '卡组分类',
              card_count  int      default 0                 null comment '卡片数量',
              create_time datetime default CURRENT_TIMESTAMP null comment '创建时间',
              update_time datetime default CURRENT_TIMESTAMP null on update CURRENT_TIMESTAMP comment '更新时间',
              status      tinyint  default 1                 null comment '状态: 0-删除, 1-正常',
              constraint flashcard_deck_ibfk_1
                  foreign key (user_id) references sys_user (id)
                      on delete cascade
          )
              comment '卡组表' charset = utf8mb4;

          create table flashcard
          (
              id               bigint auto_increment comment '卡片ID'
                  primary key,
              deck_id          bigint                                  not null comment '所属卡组ID',
              question         text                                    not null comment '问题/正面',
              answer           text                                    not null comment '答案/背面',
              next_review_date date                                    not null comment '下次复习日期',
              stage            int           default 0                 null comment '记忆阶段: 0-新卡片, 1-第1次复习, 2-第2次复习... 7-已完成',
              review_count     int           default 0                 null comment '复习次数',
              correct_count    int           default 0                 null comment '正确次数',
              wrong_count      int           default 0                 null comment '错误次数',
              difficulty       decimal(3, 2) default 1.00              null comment '难度系数: 0.50-2.00',
              create_time      datetime      default CURRENT_TIMESTAMP null comment '创建时间',
              update_time      datetime      default CURRENT_TIMESTAMP null on update CURRENT_TIMESTAMP comment '更新时间',
              status           tinyint       default 1                 null comment '状态: 0-归档, 1-学习中, 2-已掌握',
              constraint flashcard_ibfk_1
                  foreign key (deck_id) references flashcard_deck (id)
                      on delete cascade
          )
              comment '闪卡表' charset = utf8mb4;

          create index idx_deck_status
              on flashcard (deck_id, status);

          create index idx_next_review
              on flashcard (next_review_date, status);

          create index user_id
              on flashcard_deck (user_id);

          create table review_log
          (
              id                   bigint auto_increment comment '记录ID'
                  primary key,
              card_id              bigint                             not null comment '卡片ID',
              user_id              bigint                             not null comment '用户ID',
              review_date          datetime default CURRENT_TIMESTAMP null comment '复习时间',
              is_correct           tinyint                            not null comment '是否记得: 0-忘了, 1-记得',
              old_stage            int                                null comment '复习前阶段',
              new_stage            int                                null comment '复习后阶段',
              old_next_review_date date                               null comment '复习前下次复习日期',
              new_next_review_date date                               null comment '复习后下次复习日期',
              response_time        int                                null comment '响应时间(秒)',
              constraint review_log_ibfk_1
                  foreign key (card_id) references flashcard (id)
                      on delete cascade,
              constraint review_log_ibfk_2
                  foreign key (user_id) references sys_user (id)
                      on delete cascade
          )
              comment '复习记录表' charset = utf8mb4;

          create index idx_card_date
              on review_log (card_id, review_date);

          create index user_id
              on review_log (user_id);


请开始你的回答。